# bid-skills 未来优化清单

> 记录已讨论定方向、但暂未实施的优化项。每项含：背景、方案、改动点、前置条件。
> 本文档只记方向不动代码；实施时从这里取任务。

---

## 1. 章节级复用（多标场景）—— C 打底，A 增强 ✅ 已实施为 bid-recall（2026-08-25，含增强）

**实施结果**：新 skill `bid-recall` 落地，且升级为"召回+复用为主"的起草哲学——
- 召回库 = `标书/历史标书/`（人工完成、真实投标的 docx，目录即库，索引可重建）
- 章节全文 md 化（含表格还原、图片引用），`_recall-sections/` + `_recall-figures/` 图片库（跨文档哈希去重）
- 两段式召回：脚本粗筛（bigram+关键词+需求关键词计分）+ LLM 精筛 → recall-pack（人工确认）
- bid-draft 重构为改写引擎：召回打底优先（替换表+技术需求逐条差异核对+历史真实图落地），生成为兜底
- bid-check 加串味扫描（强制项）；**图片内容以召回为主，make_figures.py 已删除**（不生成示意图，无召回图宁可不配图）
- 方案 C 的人工指定 `--reuse-from` 参数不再需要（recall-pack 已覆盖且更细）；串味扫描按原设计落地

---

## 2. 素材卡语义匹配

**背景**：bid-draft 目前按关键词 grep 素材库；资格门槛（如"业绩≥200万+必须含正钩"）仍需人工翻案例卡核对。

**方案**：按评分点/资格字段自动筛选（amount_wan≥200 且 has_正钩）；bid-check 加"引用的案例卡是否满足资格字段"核对。

**改动点**：`company-knowledge` 卡片检索逻辑、`bid-check/check_bid.py` 加资格字段核对。

---

## 3. 中标标书反向学习（范文卡）—— ⚠️ 立场已变（2026-08-25）

**更新**：bid-recall 落地后，章节级索引与召回能力已具备，但召回库限定为**自家**人工真实投标稿（`标书/历史标书/`）。外部中标标书仍不入库——"只学写法不抄内容"红线保留；若未来要索引外部范文，只能做**结构参照**（章节树/篇幅分布），正文与图片不进召回库。

**背景**：95MB 中标标书可按章节类型（总体设计/实施方案/质控/进度）索引，起草时检索同型章节做结构参照——只学写法不抄内容。

**注意**：与优化 1 的复用不同——复用是改写自己的历史稿，范文是参照外部中标稿的结构。红线：不抄内容，且中标标书属外部资料，入库前确认无保密约束。

**改动点**：`company-knowledge` 加范文卡类型；`bid-draft` 上下文组装加范文检索。

---

## 4. ~~bid-check --fix 模式~~ ✅ 已完成（2026-08-20）

`check_bid.py --fix` 自动修四项低风险机械项（covers 注释/文件名 id/图注编号/图片路径），checker/fixer 配对，修复前自动备份 chapters/，报告第七节输出修复记录。废标风险、待补清单、覆盖缺口保持人工。设计要点：covers 的 tech_reqs 是起草认领制（允许父章子集），scoring 才精确一致；图注 Y 序号章内跨文件连续。已在金标准工作区注入三类故障验证修复+备份+复跑全过。

---

## 5. 官方模板锚点自动识别

**背景**：bid-export 模板复刻需人工指认章节占位。

**方案**：按标题样式提取模板章节树，与 outline 章节树自动匹配（编号+标题相似度），人工只确认匹配结果。

**改动点**：`bid-export/export_docx.py` 加模板结构分析。

---

## 6. 无线条表格 PDF 还原兜底

**背景**：pdfplumber 的 find_tables 依赖线框，无边框表格会漏。

**方案**：漏表页用 `extract_table()` 调参重试；仍失败在提取结果插 `[本页可能含无框表格，请人工核对]` 标记，不静默丢。

**改动点**：`bid-parse/extract_pdf.py`。

---

## 7. 多人协作章节合并检查

**背景**：chapters/ 按章分工 + git 协作时，合并易冲突。

**方案**：合并检查脚本：章节编号连续、图注编号不重复、待补清单汇总。

**改动点**：新脚本（可挂 bid-check 或独立工具）。

---

## 优先级建议

1（C 打底）→ 2 → 3 → 4 → 5 → 6 → 7 → 1（A 增强）

优化 1 的 C 方案在某电厂项目做完后即可实施（届时有两个同型项目可互验）。
