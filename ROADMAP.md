# bid-skills 未来优化清单

> 记录已讨论定方向、但暂未实施的优化项。每项含：背景、方案、改动点、前置条件。
> 本文档只记方向不动代码；实施时从这里取任务。

---

## 1. 章节级复用（多标场景）—— C 打底，A 增强

**背景**：某央企系电厂翻车机机器人标书技术需求高度相似（某电厂、某电厂都是摘/正/复钩 + DCS 联锁）。起草从"从零写"变成"改写+适配"可显著提效。曾讨论过建全局复用库（`~/Documents/bid-library/`）方案，因引入第二份外部状态、迁移性差被否决。

**方案 C（打底，先做）**：人工指定 + 改写模式，不做自动匹配。

- bid-draft 加 `--reuse-from <历史项目路径> [章节id列表]` 参数，如：
  `/bid-draft --reuse-from 某电厂智能解复列机器人 2.5,2.6`
- 复用模式下上下文组装改为：历史章节全文 + 新标评分原文 + 差异清单，让模型"保留结构和技术骨架，替换项目特定信息，按新评分点补强"
- 替换清单：从历史项目 bid-outline.yaml / tender-analysis.yaml 现读项目元数据（项目名、业主、工期、数量、地名），生成"旧值→新值"替换表
- bid-check 加串味扫描：正文残留历史项目特定信息（如淮北稿子里出现"某电厂"、旧工期）即报警，宁误报不漏报
- 理由：用户对自己历史稿子哪份能复用一眼就知道，自动匹配的发现性价值低；真正费时间的是改写和防串味

**方案 A（增强，后做）**：项目目录即库，现场扫描推荐。

- 不建独立库，历史项目工作区（`标书/<项目>/`）本身就是复用源
- bid-draft 检测用户未指定 `--reuse-from` 时，扫描 `标书/` 下其他项目的 chapters/ + bid-outline.yaml，按评分点/技术需求关键词交集即时生成复用推荐表（`reuse-suggestions.yaml`），人工扫一眼确认后进入改写模式
- 零外部状态：迁移时整个 `标书/` 文件夹拷走即可

**改动点**：
- `bid-draft/SKILL.md`：加复用模式说明（参数、上下文组装差异、替换清单生成）
- `bid-check/check_bid.py`：加串味扫描（读历史项目元数据生成扫描词表）
- 方案 A 另加：bid-draft 前置检查里的现场扫描推荐逻辑

**前置条件**：至少两个同型项目（某电厂已有，某电厂做完即满足）。

---

## 2. 素材卡语义匹配

**背景**：bid-draft 目前按关键词 grep 素材库；资格门槛（如"业绩≥200万+必须含正钩"）仍需人工翻案例卡核对。

**方案**：按评分点/资格字段自动筛选（amount_wan≥200 且 has_正钩）；bid-check 加"引用的案例卡是否满足资格字段"核对。

**改动点**：`company-knowledge` 卡片检索逻辑、`bid-check/check_bid.py` 加资格字段核对。

---

## 3. 中标标书反向学习（范文卡）

**背景**：95MB 中标标书可按章节类型（总体设计/实施方案/质控/进度）索引，起草时检索同型章节做结构参照——只学写法不抄内容。

**注意**：与优化 1 的复用不同——复用是改写自己的历史稿，范文是参照外部中标稿的结构。红线：不抄内容，且中标标书属外部资料，入库前确认无保密约束。

**改动点**：`company-knowledge` 加范文卡类型；`bid-draft` 上下文组装加范文检索。

---

## 4. bid-check --fix 模式

**背景**：check 目前只报问题。低风险格式项（缺图注、表头未加粗、章节编号不连续）可自动修。

**方案**：`check_bid.py --fix` 自动修低风险项；废标风险、待补清单保持人工。

**改动点**：`bid-check/check_bid.py`。

---

## 5. 官方模板锚点自动识别

**背景**：bid-export 模板复刻需人工指认章节占位。

**方案**：按标题样式提取模板章节树，与 outline 章节树自动匹配（编号+标题相似度），人工只确认匹配结果。

**改动点**：`bid-export/export_docx.py` 加模板结构分析。

---

## 6. 无线条表格 PDF 还原兜底

**背景**：pdfplumber 的 find_tables 依赖线框，无边框表格会漏。

**方案**：漏表页用 `extract_table()` 调参重试；仍失败在提取结果插 `[本页可能含无框表格，请人工核对]` 标记，不静默丢。

**改动点**：`bid-parse/extract_pdf.py`。

---

## 7. 多人协作章节合并检查

**背景**：chapters/ 按章分工 + git 协作时，合并易冲突。

**方案**：合并检查脚本：章节编号连续、图注编号不重复、待补清单汇总。

**改动点**：新脚本（可挂 bid-check 或独立工具）。

---

## 优先级建议

1（C 打底）→ 2 → 3 → 4 → 5 → 6 → 7 → 1（A 增强）

优化 1 的 C 方案在某电厂项目做完后即可实施（届时有两个同型项目可互验）。
